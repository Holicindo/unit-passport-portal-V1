[build]
builder = "NIXPACKS"
installCommand = "cd app/backend && npm install"
buildCommand = "cd app/backend && npm run build"

[deploy]
startCommand = "cd app/backend && npm run start:prod"
restartPolicyType = "on_failure"
